# ============================================================
# 同人/原创写作资料辅助Agent - Python 依赖
# ============================================================

# --- 核心: 数据模型 (零额外依赖, 始终可用) ---
pydantic>=2.8.0

# --- LLM API (生产模式需要) ---
anthropic>=0.30.0
openai>=1.50.0
python-dotenv>=1.0.0

# --- 可选: 工作流编排 ---
# langgraph>=0.2.0  # LangGraph (未安装时使用内置 SimpleWorkflowRunner)

# --- 可选: 向量数据库 + RAG ---
# chromadb>=0.5.0
# tiktoken>=0.7.0

# --- 可选: 后端 API ---
# fastapi>=0.115.0
# uvicorn[standard]>=0.30.0

# --- 开发依赖 ---
pytest>=8.0.0
